---
description: List all saved sessions
allowed-tools: [Bash]
---

List session files in `.planning/sessions/`. Run `ls -1 .planning/sessions/*.md 2>/dev/null` and display the filenames. If no files found, say "No saved sessions found."
